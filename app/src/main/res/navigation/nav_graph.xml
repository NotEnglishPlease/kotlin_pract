<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/weekForecast"
        android:name="com.example.kotlin_pract.WeekForecast"
        android:label="fragment_week_forecast"
        tools:layout="@layout/fragment_week_forecast" />
    <fragment
        android:id="@+id/mainFragment"
        android:name="com.example.kotlin_pract.MainFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_mainFragment_to_weekForecast"
            app:destination="@id/weekForecast" />
    </fragment>
</navigation>